"""Mapping models str to clasess"""

from models import Grade, Student, Subject, Teacher

MODEL_MAPPING = {
    "Grade": Grade,
    "Student": Student,
    "Subject": Subject,
    "Teacher": Teacher,
}
